<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Docs</title>
    <link rel="stylesheet" href="../style.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap" rel="stylesheet">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div class='background fixed w-full h-screen z-[2]'>
        <div class='relative w-full h-screen bg-zinc-800'>
            <!-- <div class=' flex justify-center py-4 font-semibold text-zinc-500 text-xl pt-[50px]'>Documents.</div> -->
            <h1 class='absolute top-1/2 left-1/2 -translate-x-[50%] -translate-y-[50%] text-[13vw] text-zinc-900 leading-none tracking-tight font-semibold'>Docs.</h1>
        </div>
    </div>
    <div class="foreground fixed z-[3] w-full h-full flex flex-wrap p-5 gap-5 overflow-scroll">
        
        <% notes.forEach(note => { %>
            <div class='card relative w-56 h-[264px] rounded-[42px] flex-shrink-0 bg-zinc-900 text-white py-8 px-5  overflow-hidden'>
                <!-- <FaRegFileAlt/> -->
                <p class='text-2xl text-blue-500 leading-tight'> <%= note.title %> </p>
                <div class="h-36 overflow-scroll">
                    <p class="mt-2 text-md leading-tight">
                        <%= note.desc %>
                    </p>
                </div>
                <div class='absolute bottom-0 left-0  w-full h-10 flex justify-between items-center px-5 text-sm bg-blue-700'>
                    <a href="/edit/<%= note._id %>" class="ml-4">Edit</a>
                    <a href="/delete/<%= note._id %>" class="mr-4 text-red-300">Delete</a>
                </div>
            </div>
        <% }) %>

        <a href="/createNote" class="add-btn absolute bottom-6 right-10 w-32 h-12 bg-blue-700 flex items-center justify-center rounded-[20px] text-white">Add Note ✏️</a>

    </div>
</body>
</html>